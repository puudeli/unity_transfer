# unity_transfer
Used to teleoperate the Tiago++ robot.

# Usage
1. build docker image from Dockerfile
2. export ROS_IP and ROS_MASTER_URI
3. roslaunch unity_transfer unity_transfer.launch

## args
roslaunch command accepts args for torso control.
